<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_start_draw"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#282828"
    tools:context="com.example.jagauthi.capstoneapp.StartDraw">

    <Button
        android:layout_width="120dp"
        android:layout_height="70dp"
        android:textColor="#000088"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="10dp"
        android:layout_marginTop="10dp"
        android:id="@+id/btnactivate"
        android:text="Stop Drawing" />

    <com.example.androidapp.capstoneandroidapp.DrawView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/drawview"
        android:layout_below="@+id/btnactivate" />

    <Button
        android:layout_width="100dp"
        android:layout_height="70dp"
        android:textColor="#000088"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="10dp"
        android:id="@+id/btnundo"
        android:text="Undo"
        android:layout_alignTop="@+id/btnactivate"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="120dp"
        android:layout_height="70dp"
        android:textColor="#000088"
        android:textStyle="bold"
        android:textSize="20sp"
        android:padding="10dp"
        android:id="@+id/btnsubmit"
        android:text="submit"
        android:layout_alignTop="@+id/btnactivate"
        android:layout_alignRight="@+id/drawview" />

</RelativeLayout>
